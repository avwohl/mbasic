# Include root documentation files
include README.md
include docs/user/INSTALL.md
include LICENSE
include requirements.txt

# Include the source-checkout launcher (extensionless, so no glob matches it)
include mbasic

# Include all Python source files
recursive-include src *.py

# Include UI configuration files
recursive-include src/ui *.json

# Include web backend editor assets
recursive-include src/ui/web *.js *.css

# Include the C runtime library used by --compile-c
recursive-include runtime *.c *.h

# Include documentation directory (use 'docs' not 'doc')
recursive-include docs *.md

# Include help browser search indexes
recursive-include docs/help *.json

# Include example BASIC programs
recursive-include basic *.bas *.BAS *.txt

# Include demo files
recursive-include demos *.bas *.BAS

# Exclude test and utility files from distribution
exclude tests/*
exclude utils/*
recursive-exclude tests *
recursive-exclude utils *

# Exclude git and editor files
exclude .gitignore
exclude .mbasic.conf
recursive-exclude .github *
recursive-exclude .venv-docs *

# Exclude compiled Python files
global-exclude __pycache__
global-exclude *.py[co]
global-exclude .DS_Store
global-exclude *.swp
global-exclude *~
